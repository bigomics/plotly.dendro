% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_dendro_dynamic.R
\name{add_dendro_dynamic}
\alias{add_dendro_dynamic}
\title{Add A Zoom-Driven Dendrogram Layer}
\usage{
add_dendro_dynamic(
  p,
  dendro,
  max_leaves = 300,
  budget_mode = c("pixels", "fixed"),
  min_leaf_px = 3,
  priority = c("members", "height", "hybrid"),
  min_clade = 1L,
  shape = c("wedge", "stub"),
  leaf_color = NULL,
  fill = NULL,
  line = NULL,
  orientation = "bottom",
  show_labels = TRUE,
  label_budget = 50,
  uirevision = "plotly-dendro"
)
}
\arguments{
\item{p}{A plotly object.}

\item{dendro}{A \code{\link[=dendro_data]{dendro_data()}} result carrying an \code{"hclust"} attribute.}

\item{max_leaves}{Maximum number of clades to draw at any one time.}

\item{budget_mode}{\code{"fixed"} always targets \code{max_leaves}. \code{"pixels"} (the
default) additionally caps the budget at \code{plot_px / min_leaf_px}, so a
narrow panel never draws more clades than it has room to distinguish.}

\item{min_leaf_px}{Pixels each clade needs before it is worth drawing. Only
consulted when \code{budget_mode = "pixels"}.}

\item{priority}{How to spend the budget: \code{"members"}, \code{"height"}, or
\code{"hybrid"}. See \code{\link[=dendro_cut]{dendro_cut()}}. Custom priority functions are not
supported here — see Details.}

\item{min_clade}{Stop subdividing clades below this leaf count.}

\item{shape}{Clade glyph: \code{"wedge"} or \code{"stub"}.}

\item{leaf_color}{Optional per-leaf colour, in leaf display order, used to
tint each clade by the colour that dominates it. This is what lets a
collapsed clade say \emph{what} is inside it rather than only how much.}

\item{fill}{Fallback clade fill when \code{leaf_color} is absent.}

\item{line}{A named list of plotly line properties for branches and glyph
outlines.}

\item{orientation}{One of \code{"bottom"}, \code{"top"}, \code{"left"}, or \code{"right"}.}

\item{show_labels}{Draw leaf tick labels when the zoom level leaves room.}

\item{label_budget}{Maximum leaf labels to draw at once.}

\item{uirevision}{Value held constant across updates so plotly preserves the
user's zoom.}
}
\value{
The plotly object with placeholder traces and the runtime attached.
}
\description{
Draws a dendrogram whose level of detail follows the viewport: collapsed
clades open into their sub-branches as the user zooms in, and close again as
they zoom out, holding the number of drawn clades near \code{max_leaves} at every
zoom level. Unlike a statically cut plot, zooming \emph{resolves} detail rather
than merely magnifying it.
}
\details{
Runs entirely in the browser. No Shiny session and no server round-trip are
required, so the result works in a self-contained HTML file.

The browser re-cuts the tree on \code{plotly_relayout}, restricted to the visible
leaf window, and pushes the result with \code{Plotly.react}. Because
\code{Plotly.react} emits \code{plotly_react} and never \code{plotly_relayout}, the handler
cannot re-trigger itself: there is no relayout feedback loop to guard
against. Updates are additionally debounced and skipped when the recomputed
window and budget would reproduce the cut already on screen.

Only monotone priorities are permitted. The runtime selects the visible cut
by thresholding the score, which yields a valid top-down-closed cut only
when a parent never scores below its children. All three built-in priorities
satisfy this; an arbitrary user function need not, so custom scores are
available in \code{\link[=dendro_cut]{dendro_cut()}} (static) but not here.
}
\examples{
\dontrun{
hc <- stats::hclust(stats::dist(matrix(rnorm(4000), ncol = 4)))
d <- dendro_data(hc, hang = 0.03)
plotly::plot_ly() |>
  add_dendro_dynamic(d, max_leaves = 300) |>
  add_dendro_layout(d)
}

}
\seealso{
\code{\link[=dendro_cut]{dendro_cut()}} for a static cut, \code{\link[=add_dendro_clades]{add_dendro_clades()}} for the
glyph layer it shares.
}
