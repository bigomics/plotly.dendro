% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_dendro_clades.R
\name{add_dendro_clades}
\alias{add_dendro_clades}
\title{Add Collapsed-Clade Glyph Layer}
\usage{
add_dendro_clades(
  p,
  dendro,
  orientation = "bottom",
  shape = c("wedge", "stub", "none"),
  fill = NULL,
  base = NULL,
  line = NULL,
  ...,
  inherit = FALSE
)
}
\arguments{
\item{p}{A plotly object.}

\item{dendro}{A \code{\link[=dendro_cut]{dendro_cut()}} result.}

\item{orientation}{One of \code{"bottom"}, \code{"top"}, \code{"left"}, or \code{"right"}.}

\item{shape}{Glyph shape. \code{"wedge"} (default) draws size-encoding triangles;
\code{"stub"} draws a plain line from the base to the merge height, the
convention \code{scipy} uses; \code{"none"} draws nothing, for callers that want to
render \code{dendro$clades} themselves.}

\item{fill}{Glyph fill. A single color, a vector of one color per clade (in
\code{dendro$clades} row order), or a function of the clades data frame
returning such a vector. Ignored when \code{shape} is not \code{"wedge"}.}

\item{base}{Leaf-axis baseline for the glyphs. Defaults to the minimum leaf
position in the underlying geometry, which respects \code{hang}.}

\item{line}{A named list of plotly line properties for the glyph outline.}

\item{...}{Passed to \code{plotly::add_trace()}.}

\item{inherit}{Plotly inheritance flag.}
}
\value{
A plotly object with the clade layer added.
}
\description{
Draws the collapsed frontier produced by \code{\link[=dendro_cut]{dendro_cut()}}. Each clade becomes
one glyph standing in for the subtree beneath it.
}
\details{
The default \code{"wedge"} shape is an isosceles triangle with its apex at the
clade's merge height and its base spanning the clade's true leaf range.
Because the base width \emph{is} the leaf span, glyph area encodes leaf count
with no additional scaling, and the glyph stays aligned with anything else
drawn against the leaf axis (an annotation strip, say) for free.

The apex sits at the midpoint of the leaf span rather than at the clade's
own node position. The latter is geometrically truthful — it is where the
clade attaches to its parent — but on an unbalanced clade it lands near one
end of the span, rendering a lopsided right triangle that reads as a drawing
error rather than as data.

All glyphs sharing a fill color are emitted as a \emph{single} trace, their
polygons concatenated with \code{NA} separators. Plotly's \code{fill = "toself"}
closes each gap-delimited run independently, so a few hundred clades cost
one trace per distinct color rather than one trace per clade. Trace count is
the dominant scaling factor in plotly.js, so this matters more than the
vertex count does.
}
\examples{
hc <- stats::hclust(stats::dist(matrix(rnorm(400), ncol = 4)))
d <- dendro_cut(dendro_data(hc, hang = 0.03), max_leaves = 20)
plotly::plot_ly() |>
  add_dendro_clades(d) |>
  add_dendro_segments(d)

}
\seealso{
\code{\link[=dendro_cut]{dendro_cut()}}
}
