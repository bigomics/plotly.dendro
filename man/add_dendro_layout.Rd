% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_dendro_layout.R
\name{add_dendro_layout}
\alias{add_dendro_layout}
\title{Apply Dendrogram Layout To A Plotly Object}
\usage{
add_dendro_layout(
  p,
  dendro,
  orientation = "bottom",
  show_labels = TRUE,
  height_range = c("data", "full")
)
}
\arguments{
\item{p}{A plotly object.}

\item{dendro}{A \code{\link[=dendro_data]{dendro_data()}} result.}

\item{orientation}{One of \code{"bottom"}, \code{"top"}, \code{"left"}, or \code{"right"}.}

\item{show_labels}{Logical; if \code{FALSE}, omit tick labels from the
leaf axis. Useful for subplot embedding.}

\item{height_range}{Controls the height axis range. \code{"data"} (the default)
leaves it unset so plotly autoranges to fit — with \code{hang} this yields the
fitted axis that hierarchical-clustering plots conventionally use.
\code{"full"} anchors the axis at zero, spanning \code{c(0, max_height)}. A
\code{numeric(2)} clips to an explicit range, in native (pre-orientation)
coordinates.}
}
\value{
A plotly object with layout applied.
}
\description{
Configures axis properties (tick labels, grid, zero-line) for a
dendrogram using a pre-computed \code{\link[=dendro_data]{dendro_data()}} result. Unlike
\code{\link[=add_dendro]{add_dendro()}}, this function does \strong{not} add any traces — use it
together with \code{\link[=add_dendro_traces]{add_dendro_traces()}} for full compositional control.
}
