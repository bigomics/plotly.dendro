% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dendro_cut.R
\name{dendro_cut}
\alias{dendro_cut}
\title{Reduce A Dendrogram To A Budgeted Set Of Visible Clades}
\usage{
dendro_cut(
  dendro,
  max_leaves = Inf,
  priority = c("members", "height", "hybrid"),
  min_clade = 1L
)
}
\arguments{
\item{dendro}{A \code{\link[=dendro_data]{dendro_data()}} result carrying an \code{"hclust"} attribute.}

\item{max_leaves}{Maximum number of visible clades. \code{Inf} (or any value at
or above the leaf count) returns \code{dendro} unchanged, so callers may apply
this unconditionally.}

\item{priority}{How to spend the budget. One of \code{"members"} (expand the
largest clade first — the default), \code{"height"} (expand the tallest merge
first, equivalent to \code{scipy}'s \code{truncate_mode = "lastp"}), or \code{"hybrid"}
(\code{height * log1p(members)}). May also be a function of
\verb{(members, height)} returning a numeric vector, for callers who want to
drive the cut from their own score.}

\item{min_clade}{Stop subdividing clades that already hold fewer than this
many leaves, spending the remaining budget on larger structure instead.
Note this does \emph{not} eliminate single-leaf clades: a hierarchical
clustering routinely attaches an outlier leaf at a high merge, so
splitting a large clade legitimately yields a one-member sibling. Those
singletons are data, not waste.}
}
\value{
A \code{dendro_data()}-shaped list whose \code{segments} cover only the
expanded part of the tree, plus a \code{clades} data frame describing the
collapsed frontier with columns \code{x}, \code{y}, \code{height}, \code{members}, \code{span_lo},
and \code{span_hi}. Carries a \code{"cut"} attribute recording the parameters used.
}
\description{
Collapses a dendrogram to at most \code{max_leaves} visible clades, so that a
dense tree can be drawn without overplotting. The result is itself a
\code{\link[=dendro_data]{dendro_data()}}-shaped object, so every \verb{add_dendro_*()} layer composes with
it unchanged — a cut is a transform on geometry, not a rendering mode.
}
\details{
The cut is a priority-queue peel from the root: starting from the whole tree
as a single clade, the highest-priority clade is repeatedly expanded into its
two children until the budget is reached. Because expanding only ever
replaces a clade by its own children, the expanded set is guaranteed to be a
valid top-down-closed cut for \emph{any} priority function, including non-monotone
ones.
}
\examples{
hc <- stats::hclust(stats::dist(matrix(rnorm(400), ncol = 4)))
d <- dendro_data(hc, hang = 0.03)
cut <- dendro_cut(d, max_leaves = 20)
nrow(cut$clades)

}
\seealso{
\code{\link[=add_dendro_clades]{add_dendro_clades()}} to draw the collapsed frontier.
}
